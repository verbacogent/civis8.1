name: claim-verification
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - farm-haystack[elasticsearch]  # Install the latest version of Haystack
      - transformers
      - requests
      - beautifulsoup4
      - regex
      - elasticsearch
      - faiss-cpu
      - newsapi-python
